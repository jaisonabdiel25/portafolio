# Conexión a MongoDB (fuente de los datos del portafolio, leídos en build).
# Copia este fichero a .env y rellena la connection string real.
# .env está en .gitignore: NO lo subas al repo.

MONGODB_URI=
MONGODB_DB=portfolio
